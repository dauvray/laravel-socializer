# WebRTC2 — Todo List & Analyse Architecture

> Analyse effectuée le 17 mai 2026 sur `usePeerOrchestrator` et ses composables connexes.

---

**Règles de couplage** (schéma complet des couches : [CONVENTIONS.md](CONVENTIONS.md))

- `useMediaBroadcast` n'importe **que** `usePeerOrchestrator` ; il ne touche jamais aux sous-modules ni au `peerStore`.
- `usePeerOrchestrator` est le **seul** à instancier `createPeerContext` et à composer les couches.
- Les sous-modules (`usePeerCore`, `usePeerMedia`, `usePeerConnections`, `usePeerTransport`) communiquent **uniquement** via le `context` partagé — pas d'imports croisés entre eux.
- Les couches supérieures (`useConnectionPool`, `useCallManager`, futur `useStreamManager`) reçoivent leurs dépendances **par injection descendante** depuis l'orchestrateur, et **jamais de callback vers une couche supérieure** — sinon le graphe redevient cyclique.
- `utils/` est l'infra transverse : sans état partagé, importable de partout, jamais l'inverse.
- `useSignalingQueue` détient **seule** le routage des signaux serveur entrants : aucune autre couche ne pose de `watch` sur `ctx.lastRoomSignal` — on expose un verbe et on l'inscrit dans la table `routes` de l'orchestrateur.

---

## 🧭 Méthode de travail (2026-08-14)

Le package n'accumulait pas les bugs par manque de qualité — l'architecture est saine et
documentée. Il les accumulait parce que **le seul symptôme qui casse n'était observable
par aucun test** : les incendies 🔥 ci-dessous sont tous « A diffuse, B arrive, B ne voit
rien », avec chaque fois une cause racine différente, et le mock PeerJS retournait des
connexions **orphelines** qui n'atteignaient aucun autre pair. Chaque correctif était
donc un pari, vérifié à la main — d'où trois régressions introduites *le jour même* par
le correctif précédent.

Quatre règles en découlent :

1. **Un bug vécu s'écrit d'abord dans `__tests__/scenarios/`**, en repro, **rouge avant
   le fix**. C'est le seul protocole qui n'a jamais produit de régression derrière lui
   (cf. « peerId périmé collant » : 9 tests rouges avant correctif).
2. **Asserter le fait métier, jamais l'implémentation.** `bob.receivedScreensFrom()`, pas
   « telle fonction a été appelée ». Un scénario qui passe au vert *d'emblée* est un
   mauvais signe : il ne teste pas ce qu'on croit.
3. **Un mock qui ment est pire qu'un test manquant** — il rend vert pour la mauvaise
   raison. `mockFidelity.test.js` compare mécaniquement la surface du mock à celle du
   store réel (il a immédiatement trouvé 4 membres manquants et 2 inventés). Le faux
   serveur de signalisation reproduit la **liste blanche exacte** du `UserController` :
   y ajouter un champ que le PHP ne relaie pas fabriquerait un chemin impossible en prod.
4. **Rien ne se pousse en rouge.** `hooks/pre-push` (activation :
   `git config core.hooksPath hooks`) et la CI du projet hôte
   (`.github/workflows/webrtc2-tests.yml`) lancent la suite — ~3 s.

**Gelé pendant la stabilisation** : le déménagement du routage star dans
`usePeerTransport` (item `[L]` ci-dessous). Il attend que les scénarios servent de
filet — c'est exactement le type de refacto structurelle qui a produit les régressions
du 13/08.

---

## 🟡 P2 — Améliorations (pérennisation long terme)

### createPeerContext

- [X] **`waitForMeReady()` timeout hardcodé (15s)** `[S]` : rendre configurable via options
- [X] **`session.closingUsers = new Set()` exposé directement** `[S]` : remplacer par des accesseurs pour éviter la corruption externe
- [X] **`media.videoContainer = '#videoContainer'` hardcodé** `[S]` : injecter via options pour la testabilité
- [X] **`options` obligatoire alors que la signature suggère l'inverse** `[S]` (découvert le 2026-08-13 en écrivant `createPeerContext.test.js`, **corrigé le 2026-08-14**) : `options.topology` et `options.hubSlug` étaient lus **sans** optional chaining alors que `options?.meReadyTimeoutMs` en avait un → `createPeerContext({ type, room })` jetait un `TypeError`. Aucun appelant n'était concerné (`usePeerOrchestrator` passe toujours `options`), mais l'asymétrie était un piège. **Tranché en faveur de `options = {}` dans la déstructuration** plutôt que de l'optional chaining partout : un seul endroit à tenir

### Architecture

- [X] **Séparer ConnectionPool** `[M]` : `useConnectionPool()` — retry, établissement (`requestOrConnectPeer`), recovery peer-unavailable, `syncUsersConnections`. **Fait en premier, avant CallManager** : sans lui, `useCallManager` aurait reçu un callback `requestOrConnectPeer` vers l'orchestrateur, donc un cycle. Reste à faire dans cette couche : backpressure et limites explicites (`MAX_PEERS_PER_ROOM` n'est appliqué que dans `usePeerConnections`)
- [X] **Séparer CallManager** `[L]` : `useCallManager()` — start/accept/open/stop/reset, room d'appel, retries d'invitation. Propriétaire de la FSM d'appel **sauf** les 2 mutations restées dans les handlers de stream (voir StreamManager)
- [X] **Séparer StreamManager** `[L]` : `useStreamManager()` — registre des flux distants (clé canonique, TTL, éviction FIFO), players des flux distants, départ d'un pair dont la connexion se ferme (⚠️ la **séquence** de départ est depuis remontée au CallManager — cf. « Départ d'un pair » ci-dessous ; il ne reste ici que la résolution du pair depuis `conn.metadata`). Ne touche plus `callMachine` : elle passe par les verbes `markCallConnected` / `isRemoteClosing` / `beginRemoteClosing` / `endRemoteClosing` du CallManager, qui redevient **seul propriétaire de la FSM**
- [X] **Pool d'instances Vue pour les players** `[M]` (2026-08-13) : une seule app hôte par container (`Widgets/Mediaplayer/PlayerHost.vue`) rend un `v-for` sur un registre réactif de slots détenu par `usePeerMedia`. `createVideoElement` = **acquire** (slot libre recyclé, sinon nouveau), `removeVideoElement` = **release** (slot vidé + masqué, instance conservée) → le nombre d'instances montées suit le **pic de flux simultanés**, plus le cumul de la session. Points notables : clé du `v-for` = `slot.key` (identité du slot, pas du `videoId`) sinon aucun recyclage ; le div `wrapper-${videoId}` est conservé car `v-resize` insère son propre wrapper hors virtual DOM ; point de montage en `display: contents` pour que les wrappers restent enfants flex directs de `#videoContainer` ; source de vérité de l'idempotence passée du DOM au registre ; `MediaBroadcastPlayer` réinitialise `nativeMuted` au changement de flux (instance recyclée) ; `destroyPlayers()` (teardown terminal, appelé par `cleanupPeerConnection`) est le seul chemin qui démonte réellement. 15 tests dans `__tests__/usePeerMedia.players.test.js`
- [X] **Départ d'un pair : deux chemins voisins** `[M]` (2026-08-13) : `remoteStopCall` (signal `CloseConnectionToPeerID`) et `useStreamManager.handleStreamRemoved` (fermeture de connexion PeerJS) convergent maintenant vers **`useCallManager.handleRemoteDeparture`** — séquence unique, propriétaire unique. Chaque appelant ne garde que ce qui lui appartient : validation/adaptation du payload de signalisation d'un côté, résolution du pair depuis `conn.metadata` (`_resolveRemoteSlug`, d'où le `waitForMeReady`) de l'autre. La politique est décidée par le **mode courant**, pas par le déclencheur : un seul `isCallMode = currentType !== 'stream'` gouverne à la fois la fermeture de transport et le full stop — c'est ce qui a débloqué la fusion (plus besoin de brancher par transport). **3 bugs corrigés** : (1) `handleStreamRemoved` ne coupait ni les retries ni les connexions du pair parti → sur une coupure brutale (onglet fermé, donc **sans** signal serveur) le `remotePeerId` restait enregistré et `_handleConnectionAttempt` reconnectait un pair déjà parti ; (2) la purge par `entry.metadata.from` ne matchait jamais côté **initiateur** (le flux distant arrive sur ma connexion sortante, dont `metadata.from` porte mon slug) → entrée fantôme dans `remoteStreams` ; désormais filtre sur `entry.remoteSlug` ; (3) purge limitée à la clé `slug-type` → `alice-screen` fuyait quand un pair partageait écran + caméra ; désormais tous les types du pair. **Deltas assumés** (ce n'est pas iso-comportement, d'où le report initial) : `close-call` émis avant le full stop dans les deux cas (l'ordre de la couche streams), garde `currentType !== 'stream'` appliqué aussi au chemin signal, `try/finally` sur les deux chemins (le chemin signal fuyait le garde `closingUsers` sur exception → tout départ ultérieur du pair était avalé), et full stop gardé par `canTransition` (supprime le warn `Transition invalide` du second transport). 12 tests dans `useCallManager.test.js`
- [X] **Nettoyage de la façade** (2026-08-13, avec l'extraction des couches) : retirés de `usePeerOrchestrator` **et** de `useMediaBroadcast` faute d'appelant et parce qu'ils permettaient de désynchroniser l'état — `setCurrentCallUsers`, `addCurrentCallUser`, `removeCurrentCallUser`, `clearCurrentCallUsers` (mutateurs de liste : un ajout sans invite laisse un fantôme que `stopCallWithPeers` notifie dans le vide) et `setCallInProgress` (no-op avec `true`, `callMachine.reset()` sans nettoyage des players avec `false`). Un futur appel de groupe doit passer par un verbe qui envoie l'invitation, pas par un setter de liste. `setCurrentCallRoomId` / `ensureCurrentCallRoomId` sont **conservés** : seul moyen d'imposer l'ID de room d'un appel
- [X] **Séparer SignalingQueue** `[M]` (2026-08-13) : `useSignalingQueue(ctx, { routes })`, instanciée **en dernier** dans l'orchestrateur — elle ne fait que consommer des verbes, donc sa table de routes peut pointer vers n'importe quelle couche sans callback ascendant. La table **remplace `SIGNAL_TYPES` et les deux `switch`** : un seul catalogue de types au lieu de deux sources de vérité (avant, un type listé sans `case` était ignoré en silence, un `case` sans entrée dans `SIGNAL_TYPES` était injoignable). `usePeerCore` et `usePeerConnections` n'observent plus la file ; `usePeerConnections` **n'enregistre plus aucun hook de lifecycle** (import Vue réduit à `markRaw`), donc il devient testable sans `withSetup`. **Périmètre volontairement réduit** : la sémantique de consommation reste `ctx.lastRoomSignal` (dernier signal), pas de drain — depuis instrumentée par un détecteur de trou de `seq`, voir les deux items « Détecter un signal coalescé » / « Drainer réellement la file de signaux » ci-dessous. **Deltas assumés** : (1) plus de POST `peerId: null` — garde `!getLocalPeerId` dans `responseRemotePeerConnection`, qui retourne désormais un booléen ; (2) `stopSignaling()` appelé **en tête** de `cleanupPeerConnection` — un `PEER_CONNECT_TO_REMOTE_PEER` arrivant pendant le teardown rouvrait une connexion juste après `closePeerConnection` (le watcher ne s'arrêtait qu'à `onUnmounted`) ; (3) type sans handler ou signal sans type → `warn` explicite, signal routé → `console.debug` ; (4) handler qui rejette → `console.error` au lieu d'une unhandled rejection. `roomSignals` a été **supprimé** du contexte (cf. item ci-dessous). 12 tests dans `__tests__/useSignalingQueue.test.js`

  🔥 **Régression corrigée le 2026-08-13 (même journée)** : la première version de `_route` posait deux préconditions **avant** d'appeler le handler — `await ctx.waitForMeReady()` et `if (ctx.isShuttingDown.value) return`. Symptôme : quand A diffusait sa webcam ou partageait son écran, un arrivant B ne voyait **rien**, de façon **intermittente** (le partage d'écran en premier). Cause : le chemin qui porte le média vers un arrivant est **asymétrique** — B, sans flux local, ressort de `connectToPeer` par un `return true` sans rien ouvrir (`usePeerConnections.js:196-203`), c'est donc **A** qui doit appeler `peer.call(B, monStream)`, et **`PEER_CONNECT_TO_REMOTE_PEER` n'est jamais re-livré** par l'émetteur. Un signal abandonné = un flux jamais vu. `waitForMeReady` résout instantanément quand `slug && lastLocalPeerId` sont là, mais attend **15 s puis abandonne** sinon — or `lastLocalPeerId` est remis à `null` par `_destroyPeerSingleton`, déclenché par `_schedulePeerDestroy` dès que le compteur de consommateurs passe à 0 (délai 10 s) : fenêtre atteignable quand plusieurs providers montent/démontent, et systématiquement polluée par le HMR. D'où l'aléatoire. La garde `isShuttingDown` ajoutait un second abandon, **muet**, qui frappait aussi `PEER_CONNECTION_REQUEST` (répondre son propre peerId n'ouvre pourtant aucune connexion) et pouvait rester bloquée à vie (cf. `try/finally` ci-dessous). **Règle qui en découle, désormais dans l'en-tête du composable** : le routage ne pose AUCUNE précondition et n'attend rien — les préconditions appartiennent aux handlers et au moteur de retry, qui savent réessayer, alors qu'un signal abandonné dans le routage l'est définitivement. Le cas `screen` est le plus exposé : `requestRemotePeerConnection` envoie toujours `type: currentType`, **jamais `'screen'`** (`usePeerCore.js:71`), donc la connexion d'écran vers un arrivant n'est ouverte que par le moteur de retry
- [X] **`beginShutdown` sans `endShutdown` sur exception** `[S]` (2026-08-13) : `stopWebcamStream` (`usePeerOrchestrator.js`, appelé aussi par `stopAudioStream`) et `stopCallWithPeers` (`useCallManager.js`) encadraient leur `beginShutdown()` **sans `try/finally`** — une exception dans la fenêtre laissait `shutdownCount` à ≥ 1 pour la vie du contexte. Conséquence pire qu'un simple garde bloqué : `_handleConnectionAttempt` sort par `return true`, ce qui **annule** les retries au lieu de les différer → plus aucune connexion ne se rétablit, silencieusement. Les deux ont maintenant leur `endShutdown()` dans un `finally` (dans `stopCallWithPeers`, `beginShutdown` est remonté juste avant le `try` pour rester symétrique). ⚠️ Asymétrie laissée telle quelle : `stopScreenCapture` ne pose pas le garde du tout
- [X] **`waitForMeReady` : timer de secours jamais annulé** `[S]` (2026-08-13) : `timeoutId` était assigné **après** `scope.run()`, alors que le `watchEffect` s'exécute immédiatement — sur une identité déjà prête, `_resolve(true)` faisait `clearTimeout(null)`, le timer survivait et crachait un faux `waitForMeReady a expiré après 15000 ms` 15 s plus tard sur un contexte sain (plus une fuite de timer par appel). Le `setTimeout` est désormais armé **avant** `scope.run()`
- [X] **Détecter un signal coalescé** `[S]` (2026-08-13) : le critère de réévaluation du drain (« un signal réellement observé comme perdu », ci-dessous) était **inobservable** — rien ne comptait ni ne logguait une coalescence, donc la condition ne pouvait jamais être déclenchée par une preuve. `dispatchSignal` estampille désormais chaque signal d'un `seq` monotone **par clé de file** (`signalSeq[roomId]` dans le state) et `_route` loggue tout trou dans la suite : `N signal(s) non routé(s) (seq x→y)`, enrichi des `type` manquants quand la file du store les porte encore (plafond de 10). Trois décisions à ne pas défaire : le compteur est **par room** (un compteur global créerait un trou à chaque signal d'une autre room, donc un faux positif) ; il **survit à `clearSignalQueueRoom`** (sinon le seq repartirait à 1 → rewind à gérer côté consommateur) ; et il **avance même sur un signal non routable** (type inconnu, enveloppe sans type), sinon le trou deviendrait permanent et tous les signaux suivants seraient signalés à tort. `seq` absent ⇒ détecteur inerte (enveloppes poussées hors `dispatchSignal`). **Bonus** : garde `if (!signal) return` en tête de `_route` — `clearSignalQueueRoom` est appelé **en pleine session, watcher actif** (`stopWebcamStream`, `stopCallWithPeers`), la clé disparaît, `lastRoomSignal` repasse à `null` et le routage crachait un faux `warn('signal sans type — abandonné')` à chaque arrêt de flux ou d'appel — ce bruit aurait noyé le nouveau warn. 7 tests dans `useSignalingQueue.test.js`, dont la coalescence réelle (deux `_pushSignal` dans le même tick)
- [X] **Drainer réellement la file de signaux** `[M]` — **conditionné au détecteur ci-dessus : à n'ouvrir que si le warn `signal(s) non routé(s)` apparaît réellement.** Aujourd'hui seul `at(-1)` est consommé (`ctx.lastRoomSignal`) — deux signaux dispatchés dans **le même tick** n'en déclenchent qu'un, le premier est perdu. **Vérifié sur le code le 2026-08-13, aucun chemin actuel ne produit cette condition** (à ne pas revérifier) : producteur unique sans boucle (`components/System/Notifications.vue`, un `dispatchSignal` par listener ; seul autre appelant l'exemple `StreamSimpleUI`, sur une clé `conn.peer`) ; un event Reverb = une frame WebSocket = une tâche de boucle d'événement, et les microtâches — donc le flush du `watch` — sont drainées entre deux tâches, si bien que le watcher se redéclenche pour chaque signal même pendant un `await`. La machinerie nécessaire (`seq` monotone — désormais en place —, curseur, drain sérialisé, garde de ré-entrance, détection de rewind) ne s'exercerait donc jamais en prod. Trois pièges à connaître avant de s'y remettre : (1) ne pas ré-exposer la file via `computed(() => peerStore.getQueueForRoom(contextId))` — ce computed ne trace que la *clé* `signalQueues[contextId]`, qu'un `push` ne touche pas, donc il n'est jamais invalidé et aucun `watch` ne se déclenche dessus ; c'est pourquoi `roomSignals` n'avait jamais pu être consommé et a été supprimé. Il faut watcher un **scalaire** dérivé de la file (ex. `at(-1)?.seq`) ; (2) `createMockContext._pushSignal` écrit dans `_signalQueue` (un `ref` réassigné, donc réactif par changement d'identité) alors que `getQueueForRoom` lit `_signalQueueRooms` (objet nu, non réactif) — deux structures déconnectées : **tout test de drain serait un faux positif** avant correction du mock (le détecteur, lui, lit `signal.seq` sur l'enveloppe, d'où des tests possibles sans réparer le mock) ; (3) `dispatchSignal` plafonne la file à **10** par room (`shift()`) : avec une consommation réellement complète et sérialisée (un POST par signal), une room mesh à 8 pairs génère jusqu'à 14 signaux et le plafond évincerait des signaux non encore drainés → à porter dans `webrtc2.config.js` et redimensionner en même temps. Le plafond rend aussi tout curseur basé sur `length` faux (les entrées avancent à `length` constant), d'où le `seq`. Enfin, `clearSignalQueueRoom` en pleine session (cf. détecteur) est un **rewind réel**, pas théorique
- [X] **Tests unitaires** `[M]` (2026-08-13) : requalifié depuis `[XL]` — l'étiquette datait du diagnostic « couplage fort à Vue/inject/eventBus rend le code non-testable → **extraire logique pure** ». Les extractions étant faites, il ne restait plus qu'à écrire des tests sur du code déjà découplé : **aucune ligne de production touchée** (contrôle mécanique : `git status` ne montre que `__tests__/` et les deux `.md` de suivi). **377 tests WebRTC2** en ~1,5 s. Socle complété par **112 tests** — `usePeerConnections` 47, `createPeerContext` 46, `usePeerMedia.streams` 19 — s'ajoutant à `useCallManager` 62, `useConnectionPool` 31, `usePeerCore` 27, `usePeerTransport` 24, `useSignalingQueue` 19, `useStreamManager` 19, `usePeerMedia.players` 15 et 68 tests d'`utils/`. **Prérequis traité** : `__tests__/helpers/createMockContext.js` était désynchronisé du store réel — cinq méthodes appelées par `usePeerConnections` y manquaient (`hasWaitingRemotePeerId`, `hasRemotePeerId`, `prepareRoomConnection`, `storePeerConnection`, `closePeerConnection`), et `getConnections` y était enveloppé dans un `computed()` alors que les getters Pinia sont auto-déballés et que le code lit `ctx.peerStore.getConnections?.[room]` **sans `.value`** → `hasOpenConnection` renvoyait *toujours* `false` en test (faux négatif silencieux ; les tests transport contournaient déjà le piège à la main). `removeRemotePeerId` et `clearConnectionsRoom` ont aussi été rendus fidèles (suppression conditionnelle, purge des parents devenus vides) : sans ça, l'ordre `clearConnectionsRoom` → `removeRemotePeerId` de `closePeerConnection` n'était pas observable. **Décision** : `createPeerContext` se teste avec les **vrais** stores Pinia (`setup.js` pose déjà une Pinia fraîche avant chaque test) plutôt qu'avec les 4 `vi.mock` que prévoyait `TESTS_PLAN.md` — aucun n'a d'effet de bord à l'instanciation, et on couvre au passage la vraie intégration store ↔ contexte. **Piège** : `_bindStreamCleanup` et `connectToPeer` filtrent sur `stream instanceof MediaStream`, or le flux factice de `setup.js` est un objet nu → construire de vraies instances (happy-dom expose la classe ; `MediaStreamTrack` a en revanche un constructeur illégal, d'où la surcharge de `getTracks()` sur l'instance). **Hors périmètre, volontairement** : `usePeerOrchestrator` — son wrapping du routage star doit d'abord *déménager* dans `usePeerTransport` (cf. checklist), des tests écrits maintenant seraient à jeter —, `useMediaBroadcast`, et les trous restants de `usePeerTransport` (singleton, ref-counting, reconnect, rate limiting hub) et de `usePeerCore` (`notifyCloseConnectionToPeer`, `stopCallInviteRetry*`, `onUnmounted`) ; détail dans `__tests__/TESTS_PLAN.md`. **2 bugs découverts et volontairement non corrigés** — cf. « `'vocal'` : aucune branche d'ouverture » (Robustesse) et « `options` requis » (createPeerContext)

### Observabilité

- [ ] **Ajouter un logger centralisé** : remplacer les `console.log/warn/error` dispersés par un logger configuré par composable
- [ ] **Exposer un état debug** : readonly computed pour inspecter l'état interne (retries, connections, streams)
- [ ] **Émettre des events structurés** : `peer:connected`, `peer:disconnected`, `call:started`, `call:failed`, etc.

### Sécurité

- [ ] **Rate limiting client sur les requêtes Ajax `/ask-to-peer-id`** `[S]` : `usePeerCore` appelle `ENDPOINTS.ASK_TO_PEER_ID` sans throttle côté émission — un mount/unmount rapide ou une boucle de retry peut spammer le backend. Scope distinct du rate limiting hub (`_isHubRateLimited`, qui couvre les enveloppes star data-channel sur identité PeerJS réelle). Pas un vecteur d'attaque (route authentifiée), mais protection contre le spam involontaire

### usePeerTransport

- [ ] **Variables module-level désynchronisées du store** `[M]` : `_peerConsumerCount`, `_peerInitPromise`, `_reconnectAttempts`, `_peerDestroyTimer` vivent dans le module ES et non dans Pinia — en cas de HMR, de reset du store ou de tests unitaires, ces compteurs deviennent incohérents avec l'état du `peerStore` → les déplacer dans le store ou dans une structure partagée initialisée à la création du store. ⚠️ **Contrainte de migration** : `_destroyPeerSingleton` comporte un early return (quand `localPeer` est null après un échec d'init) qui **ne remet volontairement pas `_peerConsumerCount` à 0** — les consommateurs encore montés doivent pouvoir décrémenter normalement pour un retry ; lors de la migration vers Pinia, cette asymétrie doit être préservée (ex : action `resetPeerState({ keepConsumerCount: true })`).
- [ ] **Pas de cleanup explicite des listeners Peer** `[S]` : à la destruction (`_destroyPeerSingleton`), `peer.destroy()` retire les listeners implicitement via PeerJS, mais les handlers `on('connection')`, `on('call')`, `on('disconnected')`, `on('error')`, `on('open')` ne sont jamais retirés explicitement — stocker les handlers et appeler `peer.off()` avant `peer.destroy()` pour ne pas dépendre du comportement interne de PeerJS

### usePeerConnections

- [ ] **`usersInRoom` : sémantique trompeuse (filtrage prématuré)** `[M]` : `connection.usersInRoom` stocke uniquement les *peers distants* (moi filtré à la source dans `_doGetRoomUsersDiff`) — le nom suggère "tous les users de la room" alors qu'il signifie "peers auxquels je dois me connecter" → `allUsersInRoom` n'existe que pour compenser ce filtrage prématuré (aller-retour : liste complète Reverb → retire moi → rajoute moi) ; solution : renommer `connection.usersInRoom` en `connection.remotePeers`, exposer `usersInRoom = [...remotePeers, mySlug]` (liste neutre complète) et appliquer le filtre `!== mySlug` explicitement dans la logique de connexion — supprime `allUsersInRoom` comme computed compensatoire
- [ ] **Migrer `usersInRoom` vers Pinia** `[M]` : `ctx.connection.usersInRoom` est un tableau mutable partagé hors store — le déplacer dans `peerStore` avec une action `computeRoomDiff(newSlugs)` synchrone (lecture + écriture atomique dans le store) — supprime le mutex `_diffLock` devenu inutile et rend la liste réactive dans les composants

### Robustesse

- [X] **`isShuttingDown` : garde non ré-entrant** `[S]` (2026-08-13) : `lifecycle.isShuttingDown` (booléen) → `lifecycle.shutdownCount` (compteur), `ctx.isShuttingDown` = `count > 0`. Deux arrêts concurrents ne peuvent plus se voler le garde : le premier à finir décrémente sans réautoriser les retries tant que le second est en vol. Les chemins qui appellent `beginShutdown` **sans** `endShutdown` (`cleanupPeerConnection`, `onUnmounted` du pool) gardent leur sémantique de garde permanent *sans code spécial* — un incrément jamais décrémenté maintient `count > 0` à vie. `endShutdown` a un plancher à 0 pour qu'un appel orphelin ne rende pas le compteur négatif. **Fait en prérequis de l'unification des départs de pair** : `handleRemoteDeparture` fait passer le chemin streams par l'arrêt partiel, donc par `begin`/`endShutdown` — avec un booléen, deux départs concurrents (appel à 3+) se volaient le garde
- [X] 🔥 **Régression : le partage d'écran n'atteignait plus un arrivant** `[S]` (2026-08-13) : introduite le jour même par l'item « Retry annulé » ci-dessous. Symptôme — A partage son écran, B arrive et ne voit rien, **de façon aléatoire**. Le `if (!_canEmitStreamFor(currentType)) return false` ajouté en fin de branche « type principal » de `_handleConnectionAttempt` sortait de la fonction **avant** la tentative `screen` qui suit. D'où l'aléatoire : ça ne cassait que si A n'avait pas *aussi* un flux webcam actif (sinon le prédicat passait et on tombait dans la branche `screen`). Coupure totale parce que `requestRemotePeerConnection` envoie toujours `type: currentType`, **jamais `'screen'`** — ce moteur est le **seul** ouvreur de la connexion d'écran vers un arrivant —, et parce que les deux tentatives partagent la même chaîne de retry (`usePeerRetry._retryKey` ne discrimine pas le type) : un `return` prématuré les condamne ensemble. **Correctif** : les deux tentatives sont désormais indépendantes, on accumule dans `settled` et on ne décide qu'à la fin. Corrige au passage le même défaut **préexistant** sur `if (!connected) return false` (un échec `visio` empêchait déjà la tentative `screen`). **Delta assumé** : quand A ne partage QUE son écran, le type principal n'aura jamais de flux → le retry va jusqu'à `MAX_RETRY_ATTEMPTS` puis abandonne avec un `warn`, alors que l'écran est diffusé depuis longtemps ; borné et sans conséquence fonctionnelle. 4 tests dans `useConnectionPool.test.js`, dont « partage d'écran SEUL » qui échouait — **le trou de couverture était là** : aucun test n'avait A capturant l'écran sans flux webcam
- [X] 🔥 **Arrêter la webcam coupait le partage d'écran chez les récepteurs** `[S]` (2026-08-13, **corrigé en deux passes**) : A diffuse webcam **+** écran ; A stoppe sa webcam → l'écran disparaissait aussi chez B, et un spinner d'attente le remplaçait. `useCallManager._purgePeerStreams` supprimait **toutes** les entrées de `remoteStreamsMap` du pair, quel que soit leur type : la fermeture de la connexion `stream` de A était traitée comme « A part », alors que son partage d'écran restait vivant.

  ⚠️ **La première tentative de correctif était inerte, et son test vert pour la mauvaise raison** — à lire avant de retoucher cette fonction. J'avais conditionné la purge des autres types à `connections.hasOpenConnection(userSlug, null, entry.remoteType)`. Or **`usePeerTransport` n'enregistre JAMAIS de connexion dans le store** (aucun `prepareRoomConnection` / `storePeerConnection` dans tout le fichier ; seul `usePeerConnections._saveRoomConnection` en écrit, côté **initiateur**) : pour un appel one-way entrant, le récepteur se contente de `call.answer()` + `setUpConnectionListeners()`. Côté récepteur — le seul côté dont l'affichage est en cause — ce prédicat répond donc **toujours** `false`. Le test associé passait uniquement parce que le mock fournissait une information que le vrai store ne peut pas donner ; il a été réécrit **sans aucun stub de connexion**, plus un garde `console.error` (`handleRemoteDeparture` avale ses exceptions, et une purge qui jette avant d'atteindre `alice-screen` rendait le test vert — c'est arrivé).

  **Correctif retenu** : une fermeture = **un** type retiré, sans condition. C'est sûr parce que trois choses ont changé depuis la purge élargie : chaque fermeture arrive avec son propre `conn.metadata.type` et le wrapping de `onConnectionClose` dépend du **mode du contexte** (`type === 'stream'`), pas du type de la connexion — les deux fermetures sont donc routées ; l'identification du pair passe par `entry.remoteSlug` et non plus par `metadata.from`, ce qui était la **vraie** cause de la fuite `alice-screen` que la purge élargie compensait ; et un filet indépendant des événements de fermeture a été ajouté (item suivant). **Second défaut du même scénario, côté émetteur** : `stopWebcamStream` fermait avec `clearSignalQueue: true` même pendant une capture d'écran — asymétrie inverse de `stopScreenCapture`, qui garde délibérément la file « pour le stream webcam actif » ; désormais `clearSignalQueue: !isCapturing`. 3 tests dans `useCallManager.test.js`
- [X] **Flux distants : nettoyage par fin de pistes** `[S]` (2026-08-13) : en mode `stream`, aucun player n'est créé (`useStreamManager.js`), donc `usePeerMedia._bindStreamCleanup` ne tournait pas et le registre `remoteStreamsMap` ne dépendait **que** des événements de fermeture PeerJS — sinon de l'éviction TTL, qui ne s'exécute qu'à l'arrivée d'un nouveau flux et à `STREAM_STALE_MS` (5 min). Un flux mort sans `close` laissait donc une vignette figée. `handleStreamReceived` écoute désormais `ended` / `inactive` sur les pistes du flux reçu et retire l'entrée quand le flux meurt réellement. C'est ce filet qui autorise `_purgePeerStreams` à ne retirer que le type fermé sans risque de fuite. Écouteurs `{ once: true }` et handler idempotent (l'entrée n'est supprimée que si elle porte toujours **ce** flux) : aucune désinscription à tenir, contrairement au couple `_bindStreamCleanup` / `_unbindStreamCleanup` de `usePeerMedia` qui doit gérer des instances de players recyclées. ⚠️ Garde `typeof stream.getTracks === 'function'` **en plus** de `instanceof MediaStream` : happy-dom expose la classe sans implémenter `getTracks`, et un flux peut satisfaire le type sans exposer l'API. 5 tests dans `useStreamManager.test.js`
- [X] **Spinner d'attente réapparaissant après un arrêt volontaire** `[S]` (2026-08-13) : quand un pair arrêtait sa diffusion, il redevenait « attendu » par `useAwaitedStreams` et un spinner s'affichait pendant `AWAITED_STREAM_TIMEOUT_MS`, comme si le flux allait revenir. Un pair dont un flux a **déjà** été observé n'est plus jamais attendu tant qu'il reste dans la room (mémoire `served`, réinitialisée à son départ comme `abandoned`). **Contrepartie assumée** : un pair qui arrête puis relance sa diffusion n'affiche pas de spinner pendant le rétablissement — préférable à un spinner qui semble bloqué. 2 tests dans `useAwaitedStreams.test.js`. ⚠️ **Périmé le même jour** : la mémoire `served` a disparu avec le passage à l'annonce protocolaire (item ci-dessous). L'arrêt de diffusion purge désormais l'annonce du pair, ce qui produit le même résultat **sans** la contrepartie — une relance réaffiche bien une vignette
- [X] 🔥 **peerId périmé « collant » : un arrivant ne recevait jamais le flux** `[M]` (2026-08-13) : symptôme — A diffuse sa webcam, B arrive et ne voit rien, A loggue `Erreur PeerJS : Could not connect to peer <uuid>`. À ne pas confondre avec la régression de routage du même jour, qui était intermittente et **muette** ; ici PeerJS émet un `peer-unavailable` explicite. Qu'un peerId devienne périmé est normal (chaque `new Peer()` reçoit un UUID neuf, et `PEER_DESTROY_DELAY_MS` détruit le peer d'un onglet inactif au bout de 10 s) — **le bug était que plus rien ne pouvait l'invalider ni le rafraîchir**, alors qu'un chemin de recovery existe exactement pour ça. Quatre défauts se renforçaient : (1) la recovery invalidait via `peerStore.removeRemotePeerId`, **conditionnel** — il ne supprime que si le slug n'apparaît dans aucune room ; or `System/Notifications.vue` monte en permanence un contexte `data-app` qui partage le même store Pinia, donc le pair est presque toujours présent dans `connections['app']` → **invalidation systématiquement annulée** ; (2) la recovery abandonnait sur `if (failedConns.length === 0) return` **avant** d'invalider, et ne cherchait les instances que sous `session.currentType` — jamais sous `'screen'` ; (3) `connectToPeer` refusait d'écraser un mapping existant (`if (!hasRemotePeerId)`), si bien qu'un peerId frais ouvrait bien la connexion mais laissait le store en retard ; (4) `requestOrConnectPeer` ne redemande un peerId que s'il n'en a **aucun** — avec un mapping périmé il rappelait indéfiniment un peer mort. Impasse **permanente**, pas dégradation temporaire. **Correctifs** : nouveau verbe `peerStore.invalidateRemotePeerId` (suppression inconditionnelle **+ purge du drapeau d'attente**, sans quoi la re-demande serait étranglée par le garde d'âge `SIGNALING_STALE_MS`), distinct de `removeRemotePeerId` dont la sémantique conditionnelle reste **volontaire** sur les chemins « le pair a quitté la room » ; recovery réordonnée — la résolution `peerId → slug` est désormais la seule précondition, le retrait des instances stockées balaie `currentType` **et** `'screen'` ; `connectToPeer` écrit systématiquement le peerId reçu de la signalisation. 15 tests (`usePeerTransport.peerUnavailable.test.js` 9, `peers2Store.remotePeerId.test.js` 6) + 2 dans `useConnectionPool.test.js`. ⚠️ Écrits **en repro d'abord** : 9 échouaient avant correctif. Le filet de retry manquant est l'item ci-dessous, corrigé dans la foulée
- [X] **Retry annulé alors qu'aucune connexion n'a été ouverte** `[M]` (2026-08-13) : `_handleConnectionAttempt` faisait `return true` — donc **annulait** le retry au lieu de le différer — dès que `connectToPeer` retournait `true`. Or ce `true` est ambigu : il signifie « pas d'erreur », pas « connexion ouverte ». Pour `stream` et `screen`, `connectToPeer` sort par `true` **sans rien ouvrir** quand le flux local n'est pas encore valide (seul `visio` retourne `false`). Si le flux n'était pas prêt à l'instant exact de la tentative, la connexion n'était plus jamais rouverte — cause directe du partage d'écran qui « marchait une fois sur deux », la connexion `screen` vers un arrivant n'étant ouverte **que** par ce moteur. **Des deux fix envisagés, le second a été retenu** : distinguer « rien à envoyer, abandonner » de « pas encore prêt, réessayer », via le prédicat `_canEmitStreamFor(type)` qui réplique la précondition de flux de `connectToPeer`. Le premier (« ne conclure qu'après `hasOpenConnection` ») a été **écarté** : une `DataConnection` n'est pas `open` immédiatement après `peer.connect()`, si bien que chaque tour de retry aurait rappelé `connect()` et **empilé des connexions en double** jusqu'à l'ouverture — régression pire que le bug. **Delta assumé** : un récepteur `stream` sans flux local (B, qui par design attend que A l'appelle) réessaie désormais dans le vide jusqu'à `MAX_RETRY_ATTEMPTS` (8, backoff exponentiel ≈ 55 s) puis abandonne avec un `warn` — borné, mais bruyant dans les logs. 3 tests dans `useConnectionPool.test.js`, dont la non-régression `data` (conclut après une tentative, pas d'empilement). **Corrigé dans la foulée du bug « peerId périmé collant »**, dont il était le filet de sécurité manquant
- [X] **`'vocal'` : aucune branche d'ouverture dans `connectToPeer`** `[M]` (corrigé le 2026-08-14) : fusionné avec la branche `visio`, qui a exactement les mêmes préconditions de flux (`metadataType === 'visio' || metadataType === 'vocal'`). Sans flux local, `vocal` retourne désormais `false` — et non plus `true`, qui **annulait** le retry. Le test qui figeait le comportement cassé a été inversé, plus un test « sans flux local ⇒ false ». Description d'origine ci-dessous.
  <details><summary>diagnostic initial</summary> `connectToPeer` traite `data`, `stream`, `screen` et `visio`, puis tombe sur le `return true` final — il n'existe **pas de branche `vocal`**, alors que le type est de première classe partout ailleurs : il est dans `VALID_CONNECTION_TYPES`, `_buildPeerConnectionConfig` lui affecte bien `config.stream = ctx.media.currentStream` (`:339-345`), `peers2.closePeerConnection` a son `case 'vocal'`, et `webrtc2Events.normalizeType` n'accepte que `'visio' | 'vocal'`. Conséquence pour un contexte de type `vocal` : `peer.call()` n'est **jamais** appelé, donc aucun flux ne part — et le `return true` **annule** le retry (même mécanique que l'item ci-dessous sur `stream`/`screen`), donc rien ne rattrape. Chemin concerné : `CallRemotePeerBtn` → `call-user` → `useCallManager` → `pool.requestOrConnectPeer` → `connectToPeer`. Fix probable : fusionner avec la branche `visio`, qui a exactement les mêmes préconditions de flux. ⚠️ Depuis la correction de l'item « Retry annulé » ci-dessus, `_canEmitStreamFor('vocal')` retourne `true` dès qu'un flux local est valide — le retry conclut donc, et rien ne rattrape toujours l'absence de branche. Le filet ne couvre pas ce cas : il faut bien ajouter la branche. Comportement actuel figé par un test (`vocal : type valide mais aucune branche d'ouverture`)
  </details>
- [X] 🔥 **`requestRemotePeerConnection` n'envoie jamais `type: 'screen'`** `[M]` (corrigé le 2026-08-14) — **c'était la cause racine encore ouverte du symptôme rapporté** (« A partage son écran, B arrive et ne voit rien »). Un champ `connectionType` distinct porte désormais le type réellement demandé, tandis que `type` reste le type du **contexte** : c'est la clé de routage du signal (`Notifications.vue` en dérive `roomId = '<type>-<room>'`), et y mettre `'screen'` aurait envoyé la réponse dans une file **que personne n'observe** — `'screen'` n'a pas de contexte à lui. Le champ est relayé par `UserController::askForPeerId` / `responseToPeerId` (liste blanche côté backend), renvoyé tel quel dans la réponse, et lu en priorité par `connectToPeer` ; absent ⇒ repli sur `type`, donc **rétrocompatible** avec un backend non déployé. `useConnectionPool` transmet le type dans `requestOrConnectPeer` et redemande, en cas de signalisation stale, pour **ce qui manque réellement** (type principal et/ou écran). Le garde anti-spam de `requestRemotePeerConnection` discrimine par type demandé, sans quoi la demande `'screen'` serait étranglée par celle du type principal émise juste avant. **Mesuré avant correctif** : l'écran arrivait tout de même, mais **~1,5 s plus tard et uniquement par le moteur de retry** — jamais par la signalisation ; c'est cette dépendance exclusive au retry qui l'avait rendu totalement cassable deux fois. Verrouillé par 3 scénarios bout en bout (`scenarios/lateJoiner.test.js`), **écrits en repro d'abord et rouges avant le fix**. Description d'origine ci-dessous.
  <details><summary>diagnostic initial</summary> elle poste toujours `type: ctx.session.currentType` (`usePeerCore.js:71`), et `responseRemotePeerConnection` renvoie le peerId avec ce même type. **Conséquence structurelle** : le chemin de signalisation n'ouvre *jamais* la connexion d'écran vers un arrivant — seul `_handleConnectionAttempt` le fait, via sa branche `isCapturing`. Le partage d'écran repose donc entièrement sur le moteur de retry, ce qui l'a déjà rendu fragile deux fois (cf. les deux items 🔥 ci-dessus). Effet de bord actuel : quand A ne partage que son écran, le type principal n'a jamais de flux, le retry ne « conclut » donc jamais et abandonne au bout de `MAX_RETRY_ATTEMPTS` avec un `warn` trompeur. Fix : porter le type réellement demandé dans la signalisation (une demande par type, ou un tableau de types), ce qui permettrait aussi de distinguer « n'aura jamais de flux » de « pas encore de flux » dans `_handleConnectionAttempt` et de supprimer le `warn`
  </details>
- [X] **Attente de flux : heuristique remplacée par une annonce protocolaire** `[M]` (2026-08-13) : symptôme rapporté — « le spinner s'affiche même si aucun stream n'est actif, puis disparaît au bout d'un moment ». `useAwaitedStreams` attendait **tout** pair de `usersInRoom` sans flux, faute de pouvoir savoir localement qui diffuse : tout membre non-diffuseur affichait donc une vignette pendant `AWAITED_STREAM_TIMEOUT_MS` (20 s), et un flux plus lent que ce délai n'en affichait plus. La source est désormais un fait, `ctx.media.announcedStreamsMap` (projection `announcedStreamPeers`), alimenté par **deux chemins exacts et complémentaires** :
  1. **annonce `BROADCAST_STATE` sur le data channel** — nouvelle couche `useBroadcastPresence` : émission au changement d'état local **et à l'ouverture de chaque connexion data** (le seul instant où l'on peut informer un arrivant de façon fiable ; un `watch` sur `usersInRoom` serait trop tôt, le canal n'existe pas encore). Réception interceptée dans le wrap `onDataReceived` de l'orchestrateur, donc **aucun câblage à faire côté app** — et le type ne peut pas être injecté dans un flux de chat. Identité de l'émetteur lue depuis la connexion (`utils/resolveRemoteSlug.js`, authentifiée à l'admission), **jamais** depuis le payload.
  2. **trace de l'appel one-way entrant** (`usePeerTransport`, `peer.on('call')`) : un tel appel n'existe que si l'émetteur a un flux vivant, et l'événement arrive **avant ICE**, donc avant le `stream`. C'est ce qui couvre la fenêtre « A diffuse déjà, B arrive » — que l'annonce seule ne couvre pas, la connexion data d'un contexte `stream` naissant *avec* l'appel média (cf. `connectToPeer`, qui n'ouvre `peer.connect` que si un flux local est valide).

  ⚠️ **Écart assumé avec le fix prévu ici** : le type n'est **pas** entré dans la table `routes` de `useSignalingQueue`. Cette table ne route que les enveloppes de signalisation **serveur** scopées sur `contextId` ; les enveloppes datachannel (scopées sur un peerId) en sont explicitement exclues (cf. son en-tête). L'annonce est donc traitée à l'étage infra, dans le wrap de l'orchestrateur.

  **Purge** : l'annonce d'un pair tombe à son départ ou à l'arrêt de sa diffusion (`useCallManager.handleRemoteDeparture`) et quand il quitte la room (`useBroadcastPresence`). La mémoire `served` de l'item ci-dessus **disparaît** avec sa contrepartie : un pair qui relance sa diffusion réaffiche une vignette. `AWAITED_STREAM_TIMEOUT_MS` reste comme **filet** (annonce reçue + flux qui n'arrivera jamais), et une nouvelle annonce réarme une attente abandonnée.

  **Fenêtre non couverte, et pourquoi** : entre le démarrage d'une diffusion et le premier contact P2P (échange de peerId par la signalisation + backoff de retry), rien n'est observable localement — aucun canal n'existe. Seule une annonce côté **serveur** (présence Reverb) la couvrirait ; hors périmètre front. **Limite star** : le hub retransmet `envelope.payload` tel quel, l'identité d'origine est perdue au-delà de lui — seul le hub enregistre les annonces de ses clients (même limite que `AUDIO_MUTE_TOGGLE`). 31 tests neufs (`useBroadcastPresence.test.js` 18, `usePeerOrchestrator.broadcastPresence.test.js` 6 en intégration réelle contexte + listeners, + 2 `usePeerTransport.incomingAuth`, 4 `usePeerTransport.mesh`, 1 `useCallManager`) et `useAwaitedStreams.test.js` réécrit (15). ⚠️ N'affecte **pas** l'overlay de `MediaBroadcastPlayer` (flux déjà reçu, pas encore d'image), qui s'appuie sur les events réels du `<video>` et ne devine rien
- [X] **`'audio'` absent de `VALID_CONNECTION_TYPES`** `[S]` (tranché le 2026-08-14) : `VALID_CALL_TYPES` est désormais **dérivé** de `VALID_CONNECTION_TYPES` (`[...VALID_CONNECTION_TYPES]`) — une seule source de vérité. Vérification faite avant de trancher, `'audio'` n'était émis par **aucun** appelant (seule sa propre définition le mentionnait) et `normalizeType` ne reconnaît que `'visio' | 'vocal'` : le type était **mort**, accepté à l'entrée pour être refusé à l'ouverture. Description d'origine ci-dessous.
  <details><summary>diagnostic initial</summary> `VALID_CALL_TYPES` (`utils/validators.js`, utilisé par la couche appels) accepte `'audio'`, mais `VALID_CONNECTION_TYPES` (`webrtc2.config.js`, utilisé par `usePeerConnections._buildPeerConnectionConfig` et `sanitizeMetadataType`) non → un appel de type `'audio'` passe la validation d'entrée puis se fait refuser à l'ouverture de connexion. Asymétrie historique, non fusionnée lors de l'extraction (changerait le comportement) : trancher quel jeu de types fait autorité
  </details>
- [ ] **Configurer les constantes** : `MAX_ATTEMPTS`, `STALE_MS`, `STREAM_WAIT_TIMEOUT`, `HUB_RATE_LIMIT` → dans un fichier de config WebRTC2
- [ ] **Graceful degradation eventBus** : si eventBus indisponible, logger au lieu de crash
- [ ] **Cleanup AbortController** : utiliser `AbortController` pour annuler opérations longues à la destruction

---

## 📋 Checklist de refactoring suggérée (ordre d'exécution)

```

Phase 3 — Architecture (P2)           effort / projet
──────────────────────────────────────────────────────
✅  [L]  Unifier les deux systèmes de retry (inviteRetries → usePeerRetry)
✅  [L]  Implémenter machine d'état appels (remplace callInprogress + isStoppingCall + closingUsers)
✅  [M]  Extraire useConnectionPool() — À FAIRE AVANT CallManager (sinon callback inverse = cycle)
✅  [L]  Extraire useCallManager() (start/accept/open/stop/reset), au-dessus du pool
✅  [L]  Extraire useStreamManager(), au-dessus du CallManager — FSM à propriétaire unique
✅  [M]  Pool d'instances Vue pour les players (app hôte + slots recyclés, dans usePeerMedia)
□  [L]  Déplacer routage star dans usePeerTransport (sortir de l'orchestrateur) — inclut le wrapping `onDataReceived` actuellement dans `initializePeerConnection` ; 
        nécessite un middleware/pipeline données dans `createPeerContext` ou un composable `usePeerRouter` dédié
✅  [S]  isShuttingDown : booléen → compteur (garde ré-entrant) — prérequis du point suivant
✅  [M]  Unifier les deux chemins de départ d'un pair (remoteStopCall / handleStreamRemoved)
✅  [M]  Extraire useSignalingQueue() — table de routes unique, instanciée en dernier (sémantique at(-1) conservée, désormais instrumentée)
✅  [M]  Tests unitaires du socle (usePeerConnections + createPeerContext + usePeerMedia streams) — 377 tests ;
        orchestrateur/broadcast hors périmètre tant que le routage star n'a pas déménagé (ligne ci-dessus)
□  [M]  Renommer connection.usersInRoom → remotePeers, inverser le filtre "moi" au niveau de l'usage — supprime allUsersInRoom comme computed compensatoire
□  [M]  Déplacer variables module-level (consumer count, timers) dans peerStore
□  [S]  Cleanup explicite listeners Peer avant peer.destroy()
```

**Ordre imposé** : chaque extraction doit se faire *sous* les couches qui en dépendent, jamais au-dessus. StreamManager après CallManager (il appelle `stopCallWithPeers`), CallManager après ConnectionPool (il appelle `requestOrConnectPeer`). Ce qui reste dans l'orchestrateur (≈245 lignes) : le wrapping des callbacks de connexion (routage star, chaînage des handlers de flux) et les passthroughs média — c'est le périmètre de l'item « routage star » ci-dessus.

---

## 💡 Patterns à adopter

### Injection protective

```javascript
const safeInject = (key, fallback = null) => {
    try {
        return inject(key) ?? fallback
    } catch {
        console.error(`[WebRTC2] inject('${key}') failed`)
        return fallback
    }
}
```

### Lifecycle cleanup manager

```javascript
const lifecycle = {
    watchers: [],
    timers: [],
    onWatch(stop) { this.watchers.push(stop) },
    onTimer(id) { this.timers.push(id) },
    cleanup() {
        this.watchers.forEach(w => w())
        this.timers.forEach(t => clearTimeout(t))
        this.watchers = []
        this.timers = []
    }
}
// Usage: const stop = watch(...); lifecycle.onWatch(stop)
// Destroy: onUnmounted(() => lifecycle.cleanup())
```

### Map avec TTL et limite

```javascript
const createBoundedMap = (maxSize = 1000, ttlMs = 300000) => {
    const map = new Map()
    const add = (key, value) => {
        if (map.size >= maxSize) {
            const oldest = map.keys().next().value
            map.delete(oldest)
        }
        map.set(key, { value, createdAt: Date.now() })
    }
    const get = (key) => {
        const entry = map.get(key)
        if (!entry) return null
        if (Date.now() - entry.createdAt > ttlMs) {
            map.delete(key)
            return null
        }
        return entry.value
    }
    return { add, get, delete: (k) => map.delete(k), clear: () => map.clear() }
}
```

### Machine d'état appel

```javascript
const CALL_STATES = {
    IDLE: 'idle',
    CALLING: 'calling',      // initiateur: attente réponse
    RECEIVING: 'receiving',  // récepteur: invitation reçue
    CONNECTED: 'connected',  // appel actif
    CLOSING: 'closing',      // fermeture en cours
}
// Transitions valides uniquement: IDLE→CALLING, IDLE→RECEIVING, etc.
```

---

*Document généré à partir de l'analyse de `usePeerOrchestrator.js`, `createPeerContext.js`, `usePeerCore.js`, `usePeerMedia.js`, `usePeerConnections.js`, `usePeerTransport.js`, `usePeerRetry.js`*
